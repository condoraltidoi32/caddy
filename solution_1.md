# Solution for #1: 🎯 reverse_proxy: Request body is closed before reaching error handlers on upstream dial timeout

diff --git a/modules/caddyhttp/reverseproxy/reverseproxy.go b/modules/caddyhttp/reverseproxy/reverseproxy.go
index 1234567..89abcde 100644
--- a/modules/caddyhttp/reverseproxy/reverseproxy.go
+++ b/modules/caddyhttp/reverseproxy/reverseproxy.go
@@ -45,6 +45,7 @@ type bufferedBody struct {
 	io.Reader
 	io.Closer
 	pool *sync.Pool
+	released bool
 }
 
 func (bb *bufferedBody) Close() error {
@@ -52,10 +53,18 @@ func (bb *bufferedBody) Close() error {
 		return nil
 	}
 	bb.closed = true
-	if bb.pool != nil {
+	// Do not release the buffer to the pool here; we may need to rewind it
+	// for error handlers. The buffer is released explicitly via Release().
+	return nil
+}
+
+// Release returns the underlying buffer to the pool and marks the body as released.
+func (bb *bufferedBody) Release() {
+	if bb.pool != nil && !bb.released {
 		bb.pool.Put(bb.Closer)
+		bb.released = true
 	}
-	return nil
+	bb.closed = true
 }
 
 // bufferedBody is a wrapper that allows us to read the request body
@@ -120,6 +129,7 @@ func (h *Handler) ServeHTTP(w http.ResponseWriter, r *http.Request, next caddyht
 	// buffer the request body if configured
 	var buf *bufferedBody
+	var bufferedBodyRef *bufferedBody // keep reference for rewinding and releasing
 	if h.RequestBuffers != nil && r.Body != nil && r.Body != http.NoBody {
 		buf, err = h.RequestBuffers.Get(r.Context())
 		if err != nil {
@@ -148,13 +158,11 @@ func (h *Handler) ServeHTTP(w http.ResponseWriter, r *http.Request, next caddyht
 		}
 		r.Body = buf
-		defer func() {
-			buf.Close()
-		}()
+		bufferedBodyRef = buf
+		// We'll close and release the body after the round trip or after error handling.
 	}
 
 	// Perform the round trip
 	res, err := h.roundTrip(r.Context(), w, r)
 	if err != nil {
+		// Restore the request body for error handlers.
+		// If we buffered the body, rewind it to the beginning.
+		// Otherwise, replace with http.NoBody to avoid reading from a closed body.
+		if bufferedBodyRef != nil {
+			if seeker, ok := bufferedBodyRef.Reader.(io.Seeker); ok {
+				if _, seekErr := seeker.Seek(0, io.SeekStart); seekErr == nil {
+					r.Body = bufferedBodyRef
+				} else {
+					r.Body = http.NoBody
+				}
+			} else {
+				r.Body = http.NoBody
+			}
+		} else {
+			r.Body = http.NoBody
+		}
+		// Ensure the body is closed and released after the error handler returns.
+		defer func() {
+			if bufferedBodyRef != nil {
+				bufferedBodyRef.Close()
+				bufferedBodyRef.Release()
+			}
+		}()
 		return h.handleError(w, r, err)
 	}
+	// On success, close and release the buffered body after we're done with it.
+	if bufferedBodyRef != nil {
+		bufferedBodyRef.Close()
+		bufferedBodyRef.Release()
+	}
 	// ... rest of function ...

---
_Generated by DevilX BountyHub solver_
